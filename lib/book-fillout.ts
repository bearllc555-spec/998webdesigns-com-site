/**
 * Discovery /book Fillout form — separate from SiteForge /intake.
 * Set after publishing a new Fillout form (not vcrTjtTLRtus).
 */
export const BOOK_FILLOUT_ID =
  process.env.NEXT_PUBLIC_BOOK_FILLOUT_ID?.trim() || "";
